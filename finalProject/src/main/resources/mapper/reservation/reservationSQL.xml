<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="reservation">
	<insert id="insertReservationStudio" parameterType="map">
		insert into reservation values(seq_reservation_no.nextval,#{code},#{prdNo},#{weddingDate},#{weddingTime},#{totalPrice},#{memberId},#{memberName},#{memberPhone},null,null,null,null,null,null,null,#{memberEmail},null,null,null,null,null,null,
		<choose>
			<when test="#{option1 != null}">
				#{option1}
			</when>
			<otherwise>
				null
			</otherwise>
		</choose>
		,null,null,
		<choose>
			<when test="#{option2 != null}">
				#{option2},#{option2Date},#{option2Time}
			</when>
			<otherwise>
				null,null,null
			</otherwise>
		</choose>
		,
		<choose>
			<when test="#{option3 != null}">
				#{option3}	
			</when>
			<otherwise>
				null
			</otherwise>
		</choose>
		,null,null)		
	</insert>
</mapper>
