<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="member">
	 <insert id="insertMember" parameterType="member">
     insert into member values(member_seq.nextval,#{memberId},#{memberPw},
     #{memberName},#{phone},#{birthDay},#{addr},#{marrySchedule},#{expectVisitor},
     #{budget},#{enrollDate},sysdate)
    </insert>
      <select id="selectOneMember" parameterType="member" resultType="member">
    	select 
    	member_no as memberNo,
    	member_id as memberId,
    	member_pw as memberPw,
    	member_name as memberName,
    	phone as phone,
    	birthday as birthday,
    	addr as addr,
    	marry_schedule as marrySchedule,
    	expect_visitor as expectVisitor,
    	budget as budget,
    	enrollDate as enrollDate
  	    from member where member_id=#{memberId} and member_pw=#{memberPw}
    </select>
    <select id ="selectOneStudioMember" parameterType="member" resultType="memberstudio">
    	select
    	studio_no as studioNo,
    	studio_id as studioId,
    	studio_code as studiocode,
    	studio_name as studioName,
    	studio_tel as studioTel,
    	studio_loc as studioLoc,
    	studio_addr as studioAddr,
    	studio_price as studioPrice,
    	filename as filename,
    	filepath as filepath,
    	status as status,
    	view_status as viewStatus,
    	studio_tag as studioTag,
    	hall_latitude as hallLatitude,
    	hall_longtitude as hallLongtitude
    	from studiomember where_id=#{memberId}
    </select>
     <select id ="selectOneDressMember" parameterType="member" resultType="memberdress">
     </select>
     <select id ="selectOneMakeupMember" parameterType="member" resultType="membermakeup">
     </select>
      <select id ="selectOneHallMember" parameterType="member" resultType="memberhall">
      </select>
      
    
</mapper>
