<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="simulator">
	<select id="mhSearchList" parameterType="memberhall" resultType="memberhall">
		select 
			hall_no as hallNo,
			code as code,
			hall_name as hallName,
			hall_price as hallPrice,
			hall_tel as hallTel,
			hall_addr as hallAddr,
			hall_tag as hallTag,
			hall_path as hallPath
		from
			hall
		where
			hall_loc like #{hallLoc}
	</select>
</mapper>
